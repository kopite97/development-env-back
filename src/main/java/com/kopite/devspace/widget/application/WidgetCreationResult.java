package com.kopite.devspace.widget.application;
/** Historical operation bytes and optional observation for fresh operations only. */
public record WidgetCreationResult(String body,String location,Long dataRevision) {}
